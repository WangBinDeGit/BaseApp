<?xml version="1.0" encoding="UTF-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 填充的颜色 -->
    <solid android:color="@color/backsearch" />
    <!-- 设置矩形的四个角为弧形 -->
    <!-- android:radius 弧形的半径 -->
    <corners
        android:bottomLeftRadius="@dimen/dp_35"
        android:bottomRightRadius="@dimen/dp_35"
        android:topLeftRadius="@dimen/dp_35"
        android:topRightRadius="@dimen/dp_35" />

    <stroke
        android:width="0dp"
        android:color="@color/title_color" />

</shape>