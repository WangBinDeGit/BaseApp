<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:divider">@drawable/ic_list_divider</item>
        <item name="android:dividerHeight">@dimen/division_line</item>
        <item name="android:listDivider">@drawable/ic_list_divider</item>
    </style>

    <!--时间选择空间模式-->
    <style name="customDialog" parent="@android:style/Theme.DeviceDefault.Light.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@drawable/dialog_bg</item>
    </style>

    <style name="mypopwindow_anim_style">
        <item name="android:windowEnterAnimation">@anim/photo_dialog_in_anim</item>
        <!-- 指定显示的动画xml -->
        <item name="android:windowExitAnimation">@anim/photo_dialog_out_anim</item>
        <!-- 指定消失的动画xml -->
    </style>

    <style name="AppTheme.Label.TitleNomal">
        <item name="android:textSize">@dimen/textnomal15</item>
        <item name="android:textColor">@color/textcolor3</item>
    </style>

    <style name="AppTheme.Label.ContentNomal">
        <item name="android:textSize">@dimen/textsmall</item>
        <item name="android:textColor">@color/textcolor6</item>
    </style>

    <style name="AppTheme.Label">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:textSize">@dimen/textsmall</item>
        <item name="android:textColor">@color/textcolor3</item>
    </style>


    <style name="AppTheme.Label.HouseTitle">
        <item name="android:layout_marginLeft">@dimen/dp_26</item>
        <item name="android:layout_marginRight">@dimen/dp_15</item>
    </style>

    <style name="AppTheme.Label.ContractTitle">
        <item name="android:layout_width">@dimen/dp_160</item>
        <item name="android:layout_marginLeft">@dimen/dp_25</item>
    </style>

    <style name="transparentFrameWindowStyle" parent="android:style/Theme.Dialog">
        <item name="android:windowBackground">@drawable/photo_choose_bg</item>
    </style>

    <style name="main_menu_animstyle">
        <item name="android:windowEnterAnimation">@anim/photo_dialog_in_anim</item>
        <item name="android:windowExitAnimation">@anim/photo_dialog_out_anim</item>
        <item name="android:windowSoftInputMode">stateAlwaysVisible</item>
        <!--显示软件盘-->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
    </style>

    <style name="ToolbarTheme" parent="Base.ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>

    <style name="loading_dialog" parent="Theme.AppCompat.Dialog">
        <!--是否去除标题 -->
        <item name="android:windowNoTitle">true</item>
        <!--是否去除边框-->
        <item name="android:windowFrame">@null</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否模糊-->
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowIsTranslucent">false</item><!-- 是否半透明 -->
        <item name="android:windowBackground">@color/transparent</item>
    </style>
</resources>
